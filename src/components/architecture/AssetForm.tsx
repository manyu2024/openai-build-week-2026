import { ASSET_TYPES, CRITICALITY_LEVELS } from "@/lib/constants";
export function AssetForm() { return <form className="panel p-4"><h2 className="font-medium">Add asset</h2><div className="mt-4 grid gap-3">{["Asset name", "IP / Hostname", "Zone / Segment", "OS / Platform", "Notes"].map((label) => <label key={label} className="text-xs text-slate-400">{label}<input className="mt-1 w-full rounded border bg-panel-secondary p-2 text-sm" /></label>)}<select className="rounded border bg-panel-secondary p-2 text-sm">{ASSET_TYPES.map((type) => <option key={type}>{type}</option>)}</select><select className="rounded border bg-panel-secondary p-2 text-sm">{CRITICALITY_LEVELS.map((level) => <option key={level}>{level}</option>)}</select></div></form>; }
