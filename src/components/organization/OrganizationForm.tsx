"use client";
import { FormEvent, useState } from "react";
import { useAppContext } from "@/components/providers/AppProvider";
export function OrganizationForm() { const { createOrganization } = useAppContext(); const [name, setName] = useState(""); const [description, setDescription] = useState(""); function submit(event: FormEvent) { event.preventDefault(); if (!name.trim()) return; createOrganization(name, description); window.location.assign("/architecture"); } return <section className="mx-auto max-w-xl rounded-xl border border-border bg-panel p-6"><p className="eyebrow">Organization setup</p><h1 className="mt-2 text-2xl font-semibold">Create organization</h1><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm">Organization name<input value={name} onChange={(event) => setName(event.target.value)} required className="mt-1 w-full rounded border border-border bg-panel-secondary p-2 outline-none focus:border-primary" /></label><label className="block text-sm">Description <span className="text-slate-500">(optional)</span><textarea value={description} onChange={(event) => setDescription(event.target.value)} className="mt-1 w-full rounded border border-border bg-panel-secondary p-2 outline-none focus:border-primary" rows={4} /></label><button className="rounded bg-primary px-4 py-2 text-sm text-white">Continue to Architecture</button></form></section>; }
